
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: gateways.meridio.nordix.org
spec:
  group: meridio.nordix.org
  names:
    kind: Gateway
    listKind: GatewayList
    plural: gateways
    singular: gateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.address
      name: address
      type: string
    - jsonPath: .spec.protocol
      name: protocol
      type: string
    - jsonPath: .metadata.labels.attractor
      name: attractor
      type: string
    - jsonPath: .status.status
      name: status
      type: string
    - jsonPath: .status.message
      name: message
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Gateway is the Schema for the gateways API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GatewaySpec defines the desired state of Gateway
            properties:
              address:
                description: Address of the Edge Router
                type: string
              bgp:
                description: Parameters to set up the BGP session to specified Address.
                  If the Protocol is static, this property must be empty. If the Protocol
                  is bgp, the minimal parameters to be defined in BgpSpec are RemoteASN
                  and LocalASN.
                properties:
                  bfd:
                    default: false
                    description: 'BFD monitoring of BGP session. Valid values are:
                      - false (default): no BFD monitoring - true: turns on the BFD
                      monitoring. (Currently not supported)'
                    type: boolean
                  hold-time:
                    default: 240s
                    description: 'Hold timer of the BGP session. Please refere to
                      BGP material to understand what this implies. The value must
                      be a valid duration format. For example, 90s, 1m, 1h Minimum
                      duration is 3s. Default: 240s'
                    type: string
                  local-asn:
                    description: The ASN number of the system where the FrontEnd locates
                    format: int32
                    type: integer
                  local-port:
                    default: 179
                    description: BGP listening port of the frontend. Default 179
                    type: integer
                  remote-asn:
                    description: The ASN number of the Gateway
                    format: int32
                    type: integer
                  remote-port:
                    default: 179
                    description: BGP listening port of the gateway. Default 179
                    type: integer
                required:
                - local-asn
                - remote-asn
                type: object
              protocol:
                default: bgp
                description: The routing choice between the gateway and frontend
                type: string
              static:
                description: Parameters to work with the static routing configured
                  on the Edge Router with specified Address If the Protocol is bgp,
                  this property must be empty.
                type: object
            required:
            - address
            type: object
          status:
            description: GatewayStatus defines the observed state of Gateway
            properties:
              message:
                description: Describes why Status is not "engaged"
                type: string
              status:
                description: 'If the gateway is ready to be used by the labeled attractor
                  Possible values: - disengaged: gateway is not ready to be used by
                  any attractor - engaged: gateway is ready to be used by the labeled
                  attractor - : gateway is not processed by the controller yet'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
