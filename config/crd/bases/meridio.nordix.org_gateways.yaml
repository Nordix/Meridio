
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: gateways.meridio.nordix.org
spec:
  group: meridio.nordix.org
  names:
    kind: Gateway
    listKind: GatewayList
    plural: gateways
    singular: gateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.address
      name: address
      type: string
    - jsonPath: .spec.protocol
      name: protocol
      type: string
    - jsonPath: .metadata.labels.attractor
      name: attractor
      type: string
    - jsonPath: .status.status
      name: status
      type: string
    - jsonPath: .status.message
      name: message
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Gateway is the Schema for the gateways API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GatewaySpec defines the desired state of Gateway
            properties:
              address:
                type: string
              bgp:
                properties:
                  bfd:
                    description: (optional) BFD monitoring of BGP session. Default
                      "false"
                    type: boolean
                  hold-time:
                    description: (optional) Hold timer of the BGP session. Default
                      240s
                    type: string
                  local-asn:
                    description: (mandatory) The ASN number of the system where the
                      FrontEnd locates
                    format: int32
                    type: integer
                  local-port:
                    description: (optional) BGP listening port of the FrontEnd. Default
                      179
                    type: integer
                  remote-asn:
                    description: (mandatory) The ASN number of the Gateway
                    format: int32
                    type: integer
                  remote-port:
                    description: (optional) BGP listening port of the gateway. Default
                      179
                    type: integer
                type: object
              protocol:
                type: string
              static:
                type: object
            required:
            - address
            type: object
          status:
            description: GatewayStatus defines the observed state of Gateway
            properties:
              message:
                type: string
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
