
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: attractors.meridio.nordix.org
spec:
  group: meridio.nordix.org
  names:
    kind: Attractor
    listKind: AttractorList
    plural: attractors
    singular: attractor
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.vlan-id
      name: VlanID
      type: integer
    - jsonPath: .spec.vlan-interface
      name: VlanITF
      type: string
    - jsonPath: .spec.gateways
      name: Gateways
      type: string
    - jsonPath: .status.gateways-in-use
      name: gw-in-use
      type: string
    - jsonPath: .spec.vips
      name: Vips
      type: string
    - jsonPath: .status.vips-in-use
      name: vips-in-use
      type: string
    - jsonPath: .metadata.labels.trench
      name: trench
      type: string
    - jsonPath: .status.lb-fe
      name: LB-FE
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Attractor is the Schema for the attractors API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AttractorSpec defines the desired state of Attractor
            properties:
              gateways:
                description: gateways that attractor expect to use
                items:
                  type: string
                type: array
              replicas:
                default: 1
                description: replicas of the lb-fe deployment
                format: int32
                type: integer
              vips:
                description: vips that attractor expect to use
                items:
                  type: string
                type: array
              vlan-id:
                description: (immutable) vlan ID of the vlan interface to be used
                  for external connectivity
                type: integer
              vlan-interface:
                description: (immutable) master interface of the vlan interface to
                  be used for external connectivity
                type: string
              vlan-ipv4-prefix:
                description: (immutable) ipv4 prefix of the vlan interface, which
                  is used for frontend to set up communication with the ipv4 gateways
                type: string
              vlan-ipv6-prefix:
                description: (immutable) ipv6 prefix of the vlan interface, which
                  is used for frontend to set up communication with the ipv6 gateways
                type: string
            required:
            - vlan-id
            - vlan-interface
            - vlan-ipv4-prefix
            - vlan-ipv6-prefix
            type: object
          status:
            description: AttractorStatus defines the observed state of Attractor
            properties:
              gateways-in-use:
                description: Gateways from spec.gateway that are currently used by
                  the attractor
                items:
                  type: string
                type: array
              lb-fe:
                description: 'Load balancer and front-end status. Possible values:
                  - engaged: the attractor can be used for creating resources - disengaged:
                  the attractor cannot be used for creating resources - error: there
                  is validation error in controller - : attractor is not processed
                  by the controller yet'
                type: string
              message:
                description: Describes why LbFe is disengaged
                type: string
              vips-in-use:
                description: Vips from spec.vips that are currently used by the attractor
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
