package logutils

const (
	// InterfaceObject represents an interface object
	InterfaceObject = "interfaceObject"

	// InterfaceIndex represents the index of an interface
	InterfaceIndex = "index"

	// InterfaceName represents the name of an interface
	InterfaceName = "interfaceName"

	// InterfaceType represents the type of an interface
	InterfaceType = "interfaceType"

	// Error represents an error value
	Error = "error"

	// Function represents a function name
	Function = "func"

	// Conduit represents a conduit object
	Conduit = "conduit"

	// Vips represents a list of VIPs
	Vips = "vips"

	// LBTarget represents a target for load balancer internal type
	LBTarget = "LB target"

	// Target represents a target for general logging using lb API values
	// avoid confusion with LB target
	LbAPiTarget = "LB API target"

	// Targets represents a list of targets
	Targets = "targets"

	// Target represents a target from nsp API for general logging
	Target = "target"

	// ConnectionID represents a connection ID
	ConnectionID = "connectionID"

	// PreferredInterface represents a preferred interface name
	PreferredInterface = "preferredInterface"

	// Stream represents a stream object
	Stream = "stream"

	// Identifier represents an identifier value
	Identifier = "identifier"

	// HealthService represents a health service name
	HealthService = "healthService"

	// ConnectionState represents a connection state
	ConnectionState = "connectionState"

	// ClientTarget represents a client connection target
	ClientTarget = "clientTarget"

	// Class represents a class/type name
	Class = "class"

	// Service represents a service name/endpoint
	Service = "service"

	// ConduitName represents a conduit name
	AmbassadorConduitName = "ambassadorConduit"

	// TrenchName represents a trench name
	TrenchName = "trenchName"

	// Query represents a query object
	Query = "query"

	// NumEndpoints represents number of endpoints
	NumEndpoints = "num"

	// EndpointName represents an endpoint name
	EndpointName = "endpointName"

	// Endpoint represents an endpoint object
	Endpoint = "networkserviceEndpoint"

	// NetworkService represents a network service name
	NetworkService = "networkserviceName"

	// Response represents a response object
	Response = "resp"

	// Connection represents a connection object
	Connection = "connection"
)
